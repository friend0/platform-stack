context: ./containers
build_directory: ./containers
deployment_directory: ./deployments
stack: exampleservice
components:
  - name: config
    requiredVariables:
      - ENV
  - name: app
    requiredVariables:
      - PWD
      - HOME
    exposable: true
postgres_deployment: postgres
env_prefix: examples