context: ./containers
build_directory: ./containers
deployment_directory: ./deployments
stack: app
components:
  - name: config
    requiredVariables:
      - YOU_HAVE_TO_SET_ME
  - name: app
    requiredVariables:
      - PWD
      - HOME
    exposable: true
postgres_deployment: postgres
env_prefix: examples